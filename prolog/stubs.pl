:- module(stubs, []).   % opcional: só para carregar

menu_jogo :-
    writeln('>>> (stub) menu_jogo chamado.').

imprimir_mapa :-
    writeln('>>> (stub) imprimir_mapa chamado.').

iniciar_treino(_Arg) :-
    writeln('>>> (stub) iniciar_treino chamado.').
